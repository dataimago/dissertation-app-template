# {{metadata.name}} — AI agent operating manual

> This repo was provisioned via [dissertation.ai](https://dissertation.ai). For framework-level context, see [dataimago-ai](https://github.com/dataimago/dataimago-ai), [dataimago-design](https://github.com/dataimago/dataimago-design), and [dataimago-rpkg](https://github.com/dataimago/dataimago-rpkg).

## What this repo is

A Next.js dissertation environment + Git submodule pointing at the user's R package. Together they hold the user's dissertation work — text, methodology, chapter PDFs, AI tooling context.

| Path | Purpose |
|---|---|
| `dataimago-spec.yaml` | **Source of truth** for project metadata. Edit it before changing anything else. |
| `src/app/` | Next.js app — landing page, chapter index, thesis-PDF link |
| `packages/r-packages/{{rPackage.name}}/` | The user's R package (submodule). Thesis chapters live in `ui/www/chapters/`. R code in `R/`. |
| `.github/workflows/generate.yml` | Runs `dataimago::ai(spec_path)` on push; commits generated files back |
| `.github/workflows/ci.yml` | typecheck + lint |

## The dataimago-spec.yaml is the contract

Every field in `dataimago-spec.yaml` affects something downstream. When the user edits the spec + pushes, `generate.yml` runs and regenerates derived files (types, MCP tool definitions, `.ai-context/`, etc.). **Never edit generated files directly without first updating the spec.**

The spec is documented in [dissertation-ai's design doc](https://github.com/dataimago/dissertation-ai/blob/main/docs/iteration-3-d-2-design.md) §3.

## When the user asks for help

- **"Edit chapter X"** — work in `packages/r-packages/{{rPackage.name}}/ui/www/chapters/X.qmd`. After editing, the `build-thesis.yml` workflow in the R package rebuilds the PDF.
- **"Add a committee member"** — edit `dataimago-spec.yaml`'s `vertical.dissertation.advisors.committee` array. Push.
- **"Change citation style"** — edit `dataimago-spec.yaml`'s `vertical.dissertation.thesis.bibliography.citationStyle`. Push.
- **"My institution requires a specific thesis format"** — see `packages/r-packages/{{rPackage.name}}/ui/www/THESIS-CLS-README.md` for the 3-mode strategy (shipped / vendored / generated).
- **"How do I add my IRB number?"** — edit `dataimago-spec.yaml`'s `vertical.dissertation.compliance.irb` block. The interview deliberately didn't ask for this; it's an "additional information (fill in over time)" field.

## Constraints

- **Don't delete `.ai-context/` if it exists.** It's generated by `dataimago::ai()` for AI tool consumption.
- **Don't commit secrets.** No API keys, no `.env.local`, no institutional credentials.
- **The submodule is a real Git repo.** Changes to chapter content + R code go in the R package's repo, not here. Push from there.

## Editing this CLAUDE.md

This file ships from the `dissertation-app-template` and is meant to be edited by the user as their project evolves. Add domain-specific guidance for AI agents helping with your work — e.g., your committee's preferences, your advisor's writing voice, your methodological priorities.
